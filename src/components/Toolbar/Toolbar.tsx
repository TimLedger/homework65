import React from 'react';
import './Toolbar.css';

const Toolbar: React.FC = () => {
    return (
        <nav className='main-nav'>
            <ul>
            </ul>                        
        </nav>
    );
}

export default Toolbar;
